# Observed friction log

This log records only behavior observed while initializing the canonical workspace, running the Google Agent Registry case, and exporting its public evidence.

| Stage | Observed behavior | Manual intervention | Consequence | Frequency in this case | Blocks repeatability? | Proposed bounded response | Proposed v0.2.0 issue |
|---|---|---|---|---:|---|---|---|
| Workspace resolution | No canonical workspace existed, and the first attempt correctly stopped after bounded discovery found none. | The project owner supplied one authorized path; the operator initialized it and created an ignored local pointer. | The first operational run could not begin until workspace authority was explicit. | 1 | Yes, for first-run setup | Let orchestration resolve one ignored workspace configuration and provide a read-only readiness check. | Add configured workspace resolution to `gs2c watch`. |
| Primary-source retrieval | `ingest` can preserve one supplied file, while this classification used four primary pages. | The operator downloaded the overview page, checked all four URLs, preserved one byte snapshot, and recorded three honest URL-only references. | Evidence identity is strongest for one page; the supporting source set requires manual bookkeeping. | 1 source set | Partly | Add an official-documentation source-set adapter that records resolution and preservation status per URL. | Add official documentation reference adapter. |
| Classification preparation | The strict schema worked, but the fact/inference/similarity/trend JSON was authored and checked manually. | The operator drafted the classification from the verified primary pages and validated it before qualification. | Manual JSON construction adds structural work and transcription risk. | 1 | No, but slows repetition | Add a provider-neutral interpretation proposal interface whose output remains subject to deterministic schema and state validation. | Add structured interpretation adapter output. |
| Content-input preparation | The generator accepted the seeded drafts and recorded two target-length warnings: 218-word LinkedIn analysis and 650-word essay. | The operator selected existing drafts and accepted the non-blocking warnings for this operational evidence run. | The packet is complete, but two drafts need editorial expansion before some publication contexts. | 2 warnings | No | Surface word-count readiness in one run summary; keep warnings non-authoritative and non-blocking. | Include packet warnings in run summary; no generator rule change required. |
| CLI identifier capture | Individual commands emitted IDs, but packet generation created two receipts while returning only the second run ID. | The operator parsed CLI JSON and then inspected the canonical receipt log to collect the hidden `PACKET_GENERATED` receipt. | Building a complete case index required manual cross-referencing. | 1 run | No, but error-prone | Return a structured run summary containing candidate ID, packet ID, every transition run ID, states, warnings, and manifest hash. | Add run-summary receipt/output. |
| Public evidence export | Runtime receipts contained a local execution identity, and canonical paths must not be published. | The operator copied packet bytes, replaced the local identity only in a derived index, omitted absolute paths, and retained hashes of canonical lines. | Sanitization is repeatable only with careful manual review; canonical receipts must remain untouched. | 1 export | Partly | Add a deterministic sanitized case-export profile that clearly distinguishes canonical hashes from derived records. | Add packet and receipt export profile. |
| Publication URL capture | Local release authorization does not record a later public URL. The LinkedIn URL was unavailable during this case. | The operator recorded `null`, separated reported manual publication from verified evidence, and added a follow-up. | Repository evidence cannot yet link the LinkedIn output to a public artifact. | 1 | No for governance; yes for public traceability | Add an append-only command to record a URL after manual publication without changing the original approval manifest. | Add publication URL recording. |

Observed friction items: **7**.
